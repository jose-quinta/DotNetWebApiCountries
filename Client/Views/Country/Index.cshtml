@model IEnumerable<Client.Models.Country>

@{
    ViewData["Title"] = "Countries";
}

<div class="my-3 d-md-flex justify-content-evenly">
    <div class="flex-grow-1 display-6 text-sm-center text-primary opacity-75">Country List</div>
    <div class="text-end">
        <a asp-controller="Country" asp-action="Create" class="create btn btn-info mt-1" data-toggle="modal">New Country</a>
    </div>
</div>

@if (Model.Count() < 1) {
    <h2 class="display-6 mt-3 text-center text-danger">There not are countries yet</h2>
} else {
    <div class="table-responsive">
        <table class="table table-dark table-striped table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m => m.Name)</th>
                    <th>Mantainers</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model) {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <a asp-controller="Country" asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-primary">Edit</a>
                            <a asp-controller="Country" asp-action="Details" asp-route-Id="@item.Id" class="btn btn-secondary">Details</a>
                            <a asp-controller="Country" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

@section scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
